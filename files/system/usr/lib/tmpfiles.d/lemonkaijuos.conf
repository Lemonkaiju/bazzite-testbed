# LemonKaijuOS — persistent runtime state directories
# systemd-tmpfiles creates these on first boot (they live in /var, outside the
# read-only image, so they must be declared here rather than baked in directly).
d /var/lib/lemonkaijuos        0755 root root -
d /var/lib/lemonkaijuos/wizard 0755 root root -
