name: ISO Chunk Build
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
      - name: Test
        run: echo "Clean workflow test - ready for full implementation" 